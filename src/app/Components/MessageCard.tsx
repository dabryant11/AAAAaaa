// import { Card, CardContent, Typography, Badge } from '@mui/material';

// const MessageCard = ({ message }) => {
//   return (
//     <Card>
//       <CardContent>
//         <Typography variant="h6">{message.sender}</Typography>
//         <Typography variant="body2">{message.content}</Typography>
//         <Typography variant="caption">{message.timestamp}</Typography>
//         <div>
//           {message.flaggedViolations.map((violation, index) => (
//             <Badge key={index} color="error" badgeContent={violation} />
//           ))}
//         </div>
//       </CardContent>
//     </Card>
//   );
// };
